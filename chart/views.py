from django.shortcuts import render

# Create your views here.
# from idtlit.models import ProposedAddition,

# def additionchart(request):    
    # addition = Addition.objects.filter(order=1)
    # return render(request,'chart/chart.html',{'addition':addition})
